name: "CodeQL config"

queries:
  - uses: security-extended

# Disable specific queries
query-filters:
  - exclude:
      id: gh-actions/checkout-untrusted-code
      paths:
        - .github/workflows/integration-test-reusable.yml
